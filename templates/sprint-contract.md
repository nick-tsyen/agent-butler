# Sprint Contract: <feature / sprint name>

A short agreement negotiated **before any code is written**, between the role that generates the work
and the role that evaluates it. It front-loads alignment and short-circuits vague implementation
cycles — it is the core artifact of *process observability* (it records *why* a change should be
accepted, not just *what* ran).

Keep it to one screen. Fill in all three sections.

## Scope

- Components / files this sprint may modify:
  - `<path>`
  - `<path>`

## Verification Standards

The definitive, checkable conditions for "done" this sprint:

- `<e.g. visual regression tests pass for each changed component>`
- `<e.g. main-flow end-to-end test passes>`
- `<e.g. no flash of unstyled content (FOUC)>`

## Exclusions

Explicit boundaries — what this sprint will **not** handle:

- `<e.g. not handling print styles>`
- `<e.g. not handling third-party component theming>`
