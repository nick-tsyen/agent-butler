"""
python/steps — Teaching module for agent-butler

Each step is a self-contained Python file that demonstrates a progressive
layer of agent functionality, mirroring the JavaScript step/*.js files.

  step1   — Minimal LLM streaming client
  step2   — Interactive REPL
  step3   — Tool interface + Read tool
  step4   — Agentic loop
  step5   — Core tools (Read/Write/Edit/Grep/Glob/Bash)
  step6   — Dynamic system prompt assembly
  step7   — Permission model (allow/ask/deny)
  step8   — QueryEngine for multi-turn orchestration
  step9   — Session persistence with JSONL transcripts
  step10  — Project memory with file-based long-term knowledge
  step11  — Context compaction: token estimation + micro-compact
  step12  — Token budget management: circuit breaker + truncation
  step13  — Plan Mode with EnterPlanMode/ExitPlanMode tools
  step14  — TodoWrite and session-scoped task tracking
  step15  — Persistent task graph (Task V2)
  step16  — MCP client integration
  step17  — Skills system
  step18  — Bash sandboxing with Docker support
  step19  — Sub-agents (Task tool)
  step20  — Background agents with Git worktrees
"""
